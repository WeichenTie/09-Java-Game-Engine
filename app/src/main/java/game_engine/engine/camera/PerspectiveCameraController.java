package game_engine.engine.camera;

import org.joml.Vector3f;

public class PerspectiveCameraController {

    Camera camera = new PerspectiveCamera(new Vector3f(), new Vector3f(0, 0, 1), 0);

    public PerspectiveCameraController() {
        
    }

    void update() {
        
    }
}
